{
  "name": "shitijrips-streaming",
  "version": "1.0.0",
  "description": "Telegram video streaming service with Range support for StreamVault",
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "esbuild index.ts --platform=node --format=cjs --outfile=dist/index.js",
    "start": "bun index.ts",
    "start:node": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "esbuild": "^0.20.0",
    "input": "^1.0.1",
    "telegram": "^2.26.22"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "bun-types": "^1.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
